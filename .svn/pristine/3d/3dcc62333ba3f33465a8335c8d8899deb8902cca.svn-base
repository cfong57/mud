#include <mudlib.h>

inherit OBJECT;

void extra_create() {
  set_id( ({"heart of gold", "heart"}) );
  set_name("the heart of gold");
  set_short("the heart of gold");
  set_long(wrap(
  "This is a very lifelike replica of a human heart, done in gold. It shimmers "+
  "mystically in the light, but other than that it seems perfectly ordinary.\n"));
  set_value(10000);
}