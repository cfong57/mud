#include <mudlib.h>

inherit WEAPON;

void extra_create()
{
  set_identified(1);
  set_short("old shield");
  set_id(({"old shield", "small shield", "shield"}));
  set_long("This weapon has surely seen better days. The edges are somewhat "+
  "dented, and the once-proud emblem on the front side is nearly faded. "+
  "However, it still functions fine as a shield. When you polish off some of "+
  "the grime, a nice sheen of iron can be seem.\n");
  set_name("small shield");
  set_wtype("small shield");
  set_material("iron");
  set_quality(15);
  weapon_setup();

}