#include <mudlib.h>

inherit OBJECT;

void extra_create() 
{
  set_id( ({"treant branch", "thick branch", "branch"}) );
  set_name("branch");
  set_short("thick branch from a treant");
  set_long(wrap("A thick chunk of wood, presumably taken from a deceased "+
  "treant. The wood feels strangely alive in your hands. You wonder what you "+
  "could do with this...\n"));
  set_value(500);
  set_mass(300);
  set_bulk(360);
}