#include <mudlib.h>

inherit OBJECT;

void extra_create() {
  set_id( ({"small scrap of parchment", "scrap of parchment", "parchment", "scrap", "note"}) );
  set_name("note");
  set_short("small scrap of parchment");
  set_long(wrap(
  "A note written in blood. It reads: 'Beware the ranger's remains'. "+
  "What a weird note.\n"));
  set_mass(1);
  set_bulk(1);
}